Chapter: Persistence des données
Add dependencies: Hibernate Core, PostgreSQL JDBC, HikariCP, Volley, Glide.
Create META-INF/persistence.xml with:

JDBC URL: jdbc:postgresql://localhost:5432/volunteering
User: postgres, Password: root
Dialect: PostgreSQL
hibernate.hbm2ddl.auto = update → auto-create tables
show_sql = true
here is the db info 
    private static final String DB_HOST = "192.168.1.23";
    private static final int DB_PORT = 5432;
    private static final String DB_NAME = "volenteeringApp";
    private static final String DB_USER = "mac";
    private static final String DB_PASSWORD = "omar";
    private static final String DB_URL = "jdbc:postgresql://" + DB_HOST + ":" + DB_PORT + "/" + DB_NAME;
    
Create DatabaseHelper.java with static EntityManagerFactory.
Test: insert a dummy volunteer in MainActivity.onCreate() and show Toast: "DB Connected & Table Created!"
in this project we will use mvc model so do 3 folders model view controller