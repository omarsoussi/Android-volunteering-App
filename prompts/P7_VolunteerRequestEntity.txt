Chapter: Persistence des données
Create VolunteerRequest.java (extends BaseEntity).
Fields:

title, description, location
priority → enum
needs → List<String>
volunteer → @ManyToOne
sentToOrgIds → List<Long>
status → String ("PENDING", "APPROVED", "REJECTED")
Map to table: volunteer_requests.